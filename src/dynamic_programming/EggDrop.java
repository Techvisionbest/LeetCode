package dynamic_programming;

public class EggDrop {

    /**
     *
     *
     * @param k 鸡蛋个数
     * @param n 楼层数
     * @return 最坏情况下的最少扔鸡蛋次数
     */
//    private int solution(int k, int n){
//
//    }

    /**
     * 状态转移函数
     *
     * @param k 剩余的鸡蛋个数
     * @param n 楼层数
     * @return 最坏情况下的最少扔鸡蛋次数
     */
//    private int dp(int k, int n){
//        for (int i = 1; i <= n; i++){
//            int res;
//            // 鸡蛋碎了
//            int breakCount = dp(k - 1, i -1);
//            // 鸡蛋没碎
//            int unBreakCount = dp(k, n-1);
//            res = Math.min(Math.max(breakCount, unBreakCount), res);
//        }
//    }
}
